# 网站设计稿 - 详细说明

## 📐 设计规范

### 尺寸规格

| 项目 | 桌面端 | 移动端 |
|------|--------|--------|
| 卡片宽度 | 100% (max-w-4xl) | 100% |
| 封面图宽度 | 320px (w-80) | 100% |
| 封面图高度 | 固定比例 | 48 (h-48) |
| 卡片圆角 | 16px (rounded-2xl) | 12px (rounded-xl) |
| 间距 | 32px (gap-8) | 16px (gap-4) |

---

## 🎨 色彩系统

### 主色调

```
背景色:
- 浅色模式: #ffffff (white)
- 深色模式: #111827 (gray-900)

文字色:
- 标题: #111827 (gray-900) / #f9fafb (gray-100)
- 正文: #4b5563 (gray-600) / #9ca3af (gray-400)
- 辅助: #6b7280 (gray-500) / #6b7280 (gray-500)

边框色:
- 默认: #e5e7eb (gray-200) / #374151 (gray-700)
- 悬停: #d1d5db (gray-300) / #4b5563 (gray-600)
```

### 标签渐变色

```
渐变 1 (蓝紫):
起点: #667eea
终点: #764ba2
方向: 135deg (从左上到右下)

渐变 2 (粉红):
起点: #f093fb
终点: #f5576c
方向: 135deg

渐变 3 (青蓝):
起点: #4facfe
终点: #00f2fe
方向: 135deg

使用: 轮流分配给不同的标签
```

### 阴影效果

```
默认阴影:
box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1)

悬停阴影:
box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.15)

草稿标记:
background: #eab308 (yellow-500)
color: #ffffff
```

---

## 📦 布局结构

### 桌面端布局（宽度 >= 768px）

```
┌─────────────────────────────────────────────────────────┐
│  ┌──────────┐                                      │
│  │          │  标题: Vue3 响应式原理深度解析              │
│  │  封面图  │  副标题: 从源码层面理解响应式系统              │
│  │          │                                      │
│  │ 320xauto │  描述: 本文深入剖析 Vue3 的响应式系统...         │
│  └──────────┘                                      │
│  📅 2024-01-10  ⏱️ ~8 分钟                 │
│  🏷️ [Vue] [响应式] [JavaScript]                   │
└─────────────────────────────────────────────────────────┘
```

**尺寸说明**:
- 封面图: 320px 宽度，高度自适应
- 内容区: flex-1（占据剩余空间）
- 内边距: 24px (p-6)
- 卡片间距: 32px (gap-8)

### 移动端布局（宽度 < 768px）

```
┌────────────────────┐
│                  │
│    封面图        │
│   (h-48)        │
│                  │
├────────────────────┤
│  标题            │
│  副标题          │
│  描述            │
│                  │
│  📅 日期         │
│  🏷️ 标签        │
└────────────────────┘
```

**尺寸说明**:
- 封面图: 100% 宽度，192px 高度 (h-48)
- 内边距: 24px (p-6)
- 卡片间距: 16px (gap-4)

---

## 📝 内容层级

### 字体大小

```
H1 (页面标题):
- 桌面: 32px (text-4xl)
- 移动: 24px (text-3xl)
- 粗细: 700 (font-bold)

H2 (卡片标题):
- 桌面: 24px (text-2xl)
- 移动: 20px (text-xl)
- 粗细: 700 (font-bold)
- 行数限制: 2 (line-clamp-2)

副标题:
- 桌面: 18px (text-lg)
- 移动: 16px (text-base)
- 行数限制: 2 (line-clamp-2)

正文/描述:
- 桌面: 16px (text-base)
- 移动: 14px (text-sm)
- 行数限制: 3 (line-clamp-3)

元数据 (日期/时间):
- 字体: 14px (text-sm)
- 颜色: gray-500
```

### 间距系统

```
卡片内边距: 24px (p-6)
元素间距:
  - 标题 ↔ 副标题: 8px (mb-2)
  - 副标题 ↔ 描述: 12px (mb-3)
  - 描述 ↔ 元数据: 16px (mb-4)
  - 元数据内: 8px (gap-2)

卡片间距:
  - 桌面: 32px (gap-8)
  - 移动: 16px (gap-4)
```

---

## 🏷️ 标签样式

### 标签尺寸

```
内边距:
- 水平: 12px (px-3)
- 垂直: 6px (py-1.5)

字体: 14px (text-sm)
圆角: 8px (rounded-lg)
```

### 标签分配

```
技术栈标签 (使用渐变 1):
- Vue, React, Angular, Svelte
- TypeScript, JavaScript
- Node.js, Python, Go
- Docker, Git, Nginx

概念标签 (使用渐变 2):
- 响应式, 状态管理, 性能优化
- 跨域, 安全, 鉴权
- 架构, 设计模式

主题标签 (使用渐变 3):
- 前端, 后端, 全栈
- 源码阅读, 读书笔记
- 工具, 最佳实践
```

### 标签数量限制

```
每个卡片显示: 最多 3 个标签
更多标签显示: "+N" 格式
例如: [Vue] [响应式] [JavaScript] +2
```

---

## ✨ 动画效果

### 卡片淡入动画

```css
@keyframes fadeInUp {
  from {
    opacity: 0;
    transform: translateY(20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

应用:
- 持续时间: 0.5s
- 缓动函数: ease-out
- 延迟: 每个卡片增加 0.1s
```

### 悬停效果

```
封面图缩放:
- 持续时间: 0.5s
- 缩放比例: scale(1.1)
- 过渡: ease-in-out

卡片位移:
- X 轴位移: translateX(4px)
- Y 轴缩放: scale(1.01)
- 持续时间: 0.3s

阴影加深:
- 持续时间: 0.3s
- 从 shadow-lg 到 shadow-2xl
```

### 标题颜色变化

```
默认: gray-900 (#111827)
悬停: blue-600 (#2563eb)
过渡: 0.2s
```

---

## 📸 图片处理

### 封面图尺寸

```
桌面端:
- 宽度: 320px (w-80)
- 高度: 自适应
- 对象方式: cover

移动端:
- 宽度: 100%
- 高度: 192px (h-48)
- 对象方式: cover
```

### 占位符样式

```
无封面图时显示:
- 背景: 渐变 from-blue-50 to-purple-50
- 暗色: from-blue-900/30 to-purple-900/30
- 图标: 📖 (text-6xl)
- 居中对齐
```

### 草稿标记

```
位置: 封面图右上角
- 距离: 16px (top-4 right-4)
- 内边距: 12px 6px
- 背景: yellow-500 (#eab308)
- 文字: white
- 圆角: full (rounded-full)
- 阴影: medium (shadow-md)
- 内容: "🚧 草稿"
```

---

## 🔄 响应式断点

```css
/* 移动端优先 */
默认: 移动端样式

/* 平板端 */
@media (min-width: 768px) {
  /* 使用桌面端横向布局 */
  .blog-card-inner {
    flex-direction: row;
  }
}

/* 桌面端 */
@media (min-width: 1024px) {
  /* 更大的内容区域 */
  .blog-card-inner {
    padding: 1.5rem;
  }
}
```

---

## 📋 对比表

| 特性 | 旧版本 | 新版本 |
|------|--------|--------|
| **布局** | 两列网格 | 单列大卡片 |
| **封面图** | 小图 (200px宽) | 大图 (320px宽) |
| **信息展示** | 标题+日期 | 标题+副标题+描述+标签+时间 |
| **标签样式** | 简单圆点 | 渐变色标签 |
| **动画** | 无 | 淡入+悬停效果 |
| **阅读时间** | ❌ | ✅ ~8 分钟 |
| **描述预览** | ❌ | ✅ 3 行预览 |
| **移动端** | 网格布局 | 纵向布局 |
| **视觉吸引力** | ⭐⭐ | ⭐⭐⭐⭐⭐ |

---

## 🎯 设计目标达成

### ✅ 已实现
1. 单列大卡片布局
2. 左图右文（桌面）/ 纵向（移动）
3. 完整信息展示
4. 阅读时间估算
5. 渐变色彩标签
6. 流畅动画效果
7. 响应式设计
8. 暗色模式支持

### 📐 符合规范
- TailwindCSS 工具类
- 移动优先响应式
- 无障碍友好
- 性能优化
- 可维护性高

---

## 🔧 技术实现

### 使用的技术
- **框架**: Astro 5
- **样式**: TailwindCSS 4
- **组件**: Vue 3 (islands)
- **动画**: CSS transitions
- **布局**: Flexbox + Grid

### 文件结构
```
src/
├── components/
│   ├── BlogCard.astro (单列大卡片组件)
│   └── TagsDisplay.astro (标签显示组件)
├── pages/
│   ├── blogs/
│   │   └── index.astro (博客列表页)
│   └── index.astro (首页)
└── utils/
    └── getTags.ts (标签工具)
```

---

## 💡 使用建议

### 查看效果
```bash
# 在浏览器中打开
cd ginlon-site
open design-preview.html

# 或拖拽文件到浏览器
```

### 调整设计
1. 打开 design-preview.html
2. 查看效果
3. 告诉我需要调整的地方
4. 我修改代码

### 实施到代码
确认设计后，我帮你：
1. 重构 BlogCard 组件
2. 修改博客列表页
3. 更新首页
4. 优化标签组件
5. 测试所有功能

---

**文档版本**: v2.0
**创建日期**: 2025-02-07
**状态**: 设计确认阶段
